# vello — ProviderCard

Round Robin (Ravn Design, Engineering track), Friday: a working Vello **ProviderCard** built on the
Vello Design System, with a design-fidelity audit.

```sh
npm install
npm run dev      # demo at the printed URL
npm run verify   # lint + typecheck + tests + build
```

- Component: `src/components/ProviderCard/`
- Audit: `docs/fidelity-audit.md` · Open questions: `docs/unresolved-questions.md`
- Design system (vendored, with provenance): `src/vello-ds/`
- Reference screen: `public/reference/provider-card-screenshot.png`
